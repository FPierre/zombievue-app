const connected = state => {
  return state.connected
}

const players = state => {
  return state.players
}

const undeads = state => {
  return state.undeads
}

export default {
  connected,
  players,
  undeads
}
